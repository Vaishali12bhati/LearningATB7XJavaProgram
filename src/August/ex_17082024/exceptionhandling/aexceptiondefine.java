package August.ex_17082024.exceptionhandling;

public class aexceptiondefine {
    //super parents is object
    // than throwable
    //than error and exceptions
    //that disrupt the normal flow of program
    //two typew
    //error ,exception
    //error>> stack overflow,outofmemory,halt the program
    // exception two type (checked compile time and unchecked or runtime)
    //how to handle the exception
    //handle by try and catch block
    //try catch and finally block(finally is a block of code which will execute any how )
    //purpose of try catch and finally is closing of the connection (connection can be of file , scanner )
    //try and multiple catch
    //both ar same
    // by using throw and throws


}
